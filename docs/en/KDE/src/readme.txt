The files in the "src" directory are based on the topmost source 
directory. And don't nessarily reflect the current implementation. 
Since a freeze is in affect this KDE specific documentation can only be
 merged into to topmost source tree beta release.Also it is too risky
 to make this change now.